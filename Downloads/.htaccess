# 1. Disable directory browsing
Options -Indexes

# 2. Prevent script execution (Security)
# If someone tries to upload and run a .php or .js file here, it will fail
RemoveHandler .php .phtml .php3 .php4 .php5 .php7 .phps
RemoveType .php .phtml .php3 .php4 .php5 .php7 .phps
php_flag engine off

# 3. Only allow common download file types (Optional but recommended)
# Uncomment the lines below if you want to ONLY allow specific types
# <Files *>
#     Order Allow,Deny
#     Deny from all
# </Files>
# <FilesMatch "\.(zip|exe|pdf|msi|dmg|dmg|rar|7z)$">
#     Order Deny,Allow
#     Allow from all
# </FilesMatch>
